# Contributing to ML Training

- Keep changes scoped to `projects/ml-training/` unless cross-project documentation must be updated.
- Add or update focused tests for model, training, or integration behavior.
- Run `pytest tests -q` from `projects/ml-training/` before submitting changes.
